# Homework 4

Here, we implemented a Stack data structure from scratch to simulate an `AsteroidField`. By managing an `AsteroidStack` with custom `push` and `pop` operations, we practiced LIFO (Last-In-First-Out) memory management and pointer manipulation.
