# Lab 1

This lab introduces complex 2D array manipulations and state management by creating a grid-based board game. The `Board` class handles the initialization of player positions, obstacles, and interactive objects on the grid using multidimensional arrays.
