# Lab 2

This lab focuses on File I/O and dynamic data structures like `ArrayList`. We read a text file using `Scanner` and split the contents to calculate word frequencies and build a unique word list.
