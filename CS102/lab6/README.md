# Lab 6

This lab focuses on advanced class design and implementing standard Java interfaces by creating a custom `BigInteger` class. We handled operations on very large numbers (up to 200 digits) using integer arrays and implemented the `Comparable` interface for sorting.
